{
  "name": "daily-news",
  "version": "1.0.0",
  "description": "每日新聞自動摘要 — RSS + GitHub Actions + GitHub Pages",
  "scripts": {
    "fetch": "node scripts/fetch-news.js"
  },
  "dependencies": {
    "rss-parser": "^3.13.0"
  }
}
